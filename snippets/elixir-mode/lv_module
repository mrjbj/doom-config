# key: lv
# name: lv_module
# --
defmodule LiveViewStudioWeb.${1:LiveView}Live do
  use LiveViewStudioWeb, :live_view

  def mount(_params, _session, socket) do
    {:ok, socket}
  end

  def handle_event("${2:event}", _params, socket) do
    {:noreply, socket}
  end

  def render(assigns) do
    ~H"""
    ${0}
    """
  end
end
